class Solution {
    public int maxSubArray(int[] nums) {
        int currentsum=0,maxsum=currentsum;
        for( int i=0;i<nums.length;i++){
            currentsum=currentsum+nums[i];
            if(maxsum<currentsum)
            {
                maxsum=currentsum;
            }

        }
    }
}
